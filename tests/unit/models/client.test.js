const {Client} =  require('../../../server/models/client');
const {makeClient} = require('../../seed/client');
const app       = require('../../../app');

describe('Client', () => {

  // beforeEach( async () => {
  //   await makeClient();
  // });
  //
  // afterEach( async () => {
  //   await Client.deleteMany();
  // });
  //
  // describe('..isValid', => {
  //   it('returns client with if valid id', ()=>{
  //
  //   });
  //
  //   it('returns false with if id not found', () =>{
  //
  //   });
  // });
  //


});
