<% if (admin) { %>
  <header>
    <h1><a href="/clients/new">Add a new client.</a></h1>
  </header>
<% } %>

<div class="client-list">
  <ul>
    <% clients.forEach((client) => {%>
      <li>
        <a href="/clients/<%=client._id%>" class='capitalize'><%= client.name%></a>
      </li>
    <%})%>
  </ul>
</div>
